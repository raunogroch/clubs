########################################
# Global / Docker runtime
########################################
# Mongo service (used by backend). Prefer composing URI from the parts below so
# other tools can reuse host/port separately.
MONGO_HOST=mongo
MONGO_PORT=27017
MONGO_DB=clubsdb
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=Passw0rd.123123
# Full connection string used by the backend. If you change the parts above,
# update this line or let your runtime build the URI dynamically.
MONGODB_URI=mongodb://${MONGO_INITDB_ROOT_USERNAME}:${MONGO_INITDB_ROOT_PASSWORD}@${MONGO_HOST}:${MONGO_PORT}/${MONGO_DB}?authSource=admin

########################################
# Backend (NestJS)
########################################
JWT_SECRET=anitalavalatina
BACKEND_PORT=3000
IMAGE_PROCESSOR_API=https://images.ranr.top
# App environment
NODE_ENV=production
# Optional: log level (debug, info, warn, error)
LOG_LEVEL=info

########################################
# Image Processor
########################################
# Port the image processor listens on
IMAGE_PROCESSOR_PORT=4000

########################################
# Frontend (Vite) — client-exposed variables must start with VITE_
########################################
# Host port where nginx (serving the built frontend) is exposed on the host
FRONTEND_PORT=5000
VITE_BACKEND_URI=https://backend.ranr.top
VITE_IMAGE_PROCESSOR_API=https://images.ranr.top
VITE_FILES_PROCESSOR_API=https://images.ranr.top

########################################
# Docker / Compose ports (host mappings)
########################################
MONGODB_PORT=27017

